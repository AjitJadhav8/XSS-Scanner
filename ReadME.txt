# XSS-Scanner
Cross-Site Scripting (XSS) attack is type of injection, in which malicious scripts are injected into websites.
This tools is helpfull for Information Gathering as well as for find bugs on web.

1) Import all libraries on pycharm/terminal byt typing 

pip install requests
pip install bs4
pip install colorama

2) Give path on terminal

after ruunning file first type website domain
if there is login page then you get login and password type valid username and password
